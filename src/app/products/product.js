"use client";

import React from "react";

const Product22 = ({ price }) => {
  return (
    <div>
      <button onClick={() => alert(price)}>want to know the Price</button>
    </div>
  );
};

export default Product22;
